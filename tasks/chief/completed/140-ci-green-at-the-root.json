{
  "project": "amphora",
  "category": "fix",
  "branchName": "chief/140-ci-green-at-the-root",
  "touches": [
    "ios",
    "android",
    ".github",
    ".chief",
    "docs"
  ],
  "iters": 7,
  "warmup": [
    "gh run view 34556064209 --log-failed 2>/dev/null | grep -oE '[A-Za-z]+\\.swift:[0-9]+:[0-9]+: error: .*' | sort -u || echo 'gh unavailable'",
    "swift --version 2>&1 | head -1 || true",
    "swift build --package-path ios -Xswiftc -warnings-as-errors 2>&1 | tail -2 || true",
    "grep -n 'swift build' .chief/verify.sh .github/workflows/ci.yml || true",
    "grep -n 'room.schemaLocation\\|exportSchema' android/build.gradle.kts android/src/main/kotlin/dev/amphora/store/UploadDatabase.kt || true",
    "ls android/schemas 2>/dev/null || echo 'android/schemas: ABSENT (not in git)'",
    "gh run view 33044503283 --log-failed 2>/dev/null | grep -m3 -E 'Empty schema file|SchemaBundle' || true"
  ],
  "description": "**ROADMAP Phase 1, the gate itself. CI has been red on every run in its life.** Re-observed 2026-09-11 on run `34556064209` (main, head `5ac68e3`): `ios` FAILURE; `android`, `react-native`, `conformance-fixture`, `verify-policy` SUCCESS. The `ios` job dies in its FIRST step (`swift build --package-path ios -Xswiftc -warnings-as-errors`, `emit-module command failed`) on exactly two errors:\n\n- `ios/Sources/Amphora/Governor/NetworkGovernor.swift:23:26: error: reference to captured var 'self' in concurrently-executing code`\n- `ios/Sources/Amphora/Transport/TUSKitTransport.swift:27:17: error: stored property 'session' of 'Sendable'-conforming struct 'TUSKitTransport' has non-sendable type 'BackgroundSessionManager'`\n\nso `swift run AmphoraPathTests` and the Swift drift control have never once executed in CI.\n\n**Why the local gate cannot see them — measured while authoring, 2026-09-11.** The local toolchain is Swift 6.3.3 (macOS 26 SDK); CI is `macos-14-arm64` (image 20260831.0302) with its default Xcode. A CLEAN local `swift build --package-path ios -Xswiftc -warnings-as-errors` exits 0. Adding `-Xswiftc -strict-concurrency=complete` locally surfaces the same defect from the other side: `BackgroundSessionManager.swift:29:17: error: stored property 'systemCompletionHandler' of 'Sendable'-conforming class 'BackgroundSessionManager' is mutable` (plus an unrelated `SQLiteUploadStore.swift:42:28` deinit finding). The likely reading — an inference, not verified — is that the newer SDK makes the class implicitly `Sendable` through `URLSessionDelegate`, so the struct error vanishes locally and reappears as a mutability error under complete checking, while on CI's older SDK the class is not `Sendable` at all. Either way there is one root: `BackgroundSessionManager` has mutable state (`systemCompletionHandler`, the `lazy var session`) and no concurrency model. ROADMAP §4 records that model as owned by nobody and says Phase 1 forces the answer. This tasklist is the answer.\n\n**Must NOT depend on ADR-0002.** Whether iOS 17 becomes the minimum — which would delete `TUSKitTransport` — is an open operator decision. The fix holds at the current `platforms: [.iOS(.v15), .macOS(.v14)]` with `TUSKitTransport` in place; if ADR-0002 later deletes it, nothing here should need undoing.\n\n**The android job is green today, and that proves nothing about the kapt failure.** `:android:kaptReleaseKotlin` failed with `java.lang.IllegalStateException: Empty schema file` at `androidx.room.migration.bundle.SchemaBundle$Companion.deserialize(SchemaBundle.kt:69)` <- `androidx.room.vo.Database.exportSchema(Database.kt:110)` on run `33044503283`, and passed on inputs differing by one JSON file. The ROADMAP exit rule is explicit: a green run after a red one on unchanged input closes nothing. Likely mechanism — NOT verified, US-2 must confirm or refute it: `android/build.gradle.kts` (~line 85) passes `room.schemaLocation = $projectDir/schemas` to kapt; `android/schemas` is not in git; `:android:assemble` runs `kaptDebugKotlin` and `kaptReleaseKotlin` concurrently (run 34556064209 starts them at 02:51:02.2888 and 02:51:02.2893); both export `dev.amphora.store.UploadDatabase/1.json` into the one directory, and one can deserialize a file the other has created but not yet written. Room's documentation says the schema directory must be declared to Gradle as a task input/output and that exported schemas belong in version control; the Room Gradle Plugin (`androidx.room`, since Room 2.6.0, configures javac, KAPT and KSP) exists to do both.\n\n**What this tasklist cannot do on its own: close Phase 1.** Closing needs a run on `main`, and chief merges locally without pushing. US-3 states exactly what the operator must do.",
  "userStories": [
    {
      "id": "US-1",
      "title": "BackgroundSessionManager gets a concurrency model, and both ios compile errors go away because of it — not because a diagnostic was silenced",
      "acceptanceCriteria": [
        "Before touching code, record the before-state in notes: the two error lines from `gh run view 34556064209 --log-failed`, verbatim, and the diagnostic list of a CLEAN local `swift build --package-path ios --scratch-path <fresh tmp dir> -Xswiftc -warnings-as-errors -Xswiftc -strict-concurrency=complete`. The fresh `--scratch-path` matters: an incremental build prints no diagnostics for files it does not recompile, which reads as clean.",
        "`BackgroundSessionManager` carries a written concurrency model on the type: every stored property, whether it is mutable, and which threads or queues touch it (the session's delegate queue, `DispatchQueue.main` in `urlSessionDidFinishEvents`, arbitrary callers of `setSystemCompletionHandler`). Every mutable property is made immutable or guarded by a lock available at the CURRENT minimums — iOS 15 / macOS 14, so `NSLock` or equivalent, not `OSAllocatedUnfairLock` (iOS 16+). Only then is `Sendable` declared on the class; `@unchecked Sendable` is acceptable if and only if the doc comment justifies each stored property. Declaring `Sendable` over an unguarded `var` is the defect restated, not the fix.",
        "`NetworkGovernor.start()` no longer references a captured `var` inside the `Task` (the `[weak self]` capture is that var). The actor -> `NWPathMonitor` -> `pathUpdateHandler` -> actor edge stays weak: the monitor is owned by the actor, so a strong capture there is a retain cycle that `stop()` does not break. State in notes how the new code keeps it weak.",
        "No dependency on ADR-0002: `git diff` shows `ios/Package.swift`'s `platforms:` unchanged, `TUSKitTransport.swift` still present and still returned by `TransportSelector.select` below iOS 17, and no `@available` / `#available` bump introduced to dodge a diagnostic.",
        "None of these silencers appear in the diff: removing or weakening `-warnings-as-errors`; `@preconcurrency import`; `nonisolated(unsafe)`; `-strict-concurrency=minimal`; a `swiftLanguageVersions` / `swiftSettings` change; a `#if compiler(...)` guard around the affected code. Each turns the job green without answering who owns the concurrency model.",
        "`BackgroundUploadStarter` (in `NativeResumableTransport.swift`) is reconciled with the new model — either kept with its doc comment corrected (it is no longer the only `Sendable` claim, and 'the Sendable redesign `BackgroundSessionManager` still needs' stops being true) or removed. Either way the line `        return TransferHandle(taskIdentifier: taskId, stagedRemainderPath: nil)` in `NativeResumableTransport.swift` stays byte-identical: it is the anchor of the `swift / I6` control in `Tests/Conformance/drift-control.sh`, and a missing anchor fails that script.",
        "`.chief/verify.sh` builds Swift with the flag CI uses (`-Xswiftc -warnings-as-errors`), closing the first bullet of docs/reference/continuous-integration.md § Known divergence as far as flags can; `.chief/tests/verify-skip-policy.sh` still passes. The `ios` CI job gains a first step printing `swift --version` and `xcodebuild -version`, so the next divergence names the compiler that produced it — run 34556064209's log does not.",
        "After the fix, re-run the same clean complete-checking build and record its diagnostic list. `BackgroundSessionManager.swift:29` must be gone. `SQLiteUploadStore.swift:42:28` (`sqlite3_close(database)` in a nonisolated `deinit`) is a separate pre-existing finding: fix it or record it as out of scope with its exact text; it counts neither for nor against this story. Do not add `-strict-concurrency=complete` to CI unless the build is clean under it.",
        "GATES: `swift build --package-path ios -Xswiftc -warnings-as-errors` exit 0; `swift run --package-path ios AmphoraPathTests` exit 0 printing `46 passed`; `Tests/Conformance/drift-control.sh --port swift` ending `2 control(s) ran, 0 skipped, 0 failure(s)`; `.chief/tests/verify-skip-policy.sh` exit 0; `.chief/verify.sh` exit 0 with its `Gradle build — no JDK on PATH` skip named as NOT green (CLAUDE.md §3). A local build cannot prove the CI errors are gone — CI's compiler is not this one, as measured above. That proof belongs to US-3."
      ],
      "passes": true,
      "notes": "BEFORE. gh run view 34556064209 --log-failed, verbatim, two errors: `NetworkGovernor.swift:23:26: error: reference to captured var 'self' in concurrently-executing code`; `TUSKitTransport.swift:27:17: error: stored property 'session' of 'Sendable'-conforming struct 'TUSKitTransport' has non-sendable type 'BackgroundSessionManager'`. Clean local `swift build --package-path ios --scratch-path /tmp/amphora-scratch-d2F1tA -Xswiftc -warnings-as-errors -Xswiftc -strict-concurrency=complete` (fresh scratch, exit 1) diagnosed TWO, neither of them the CI pair: `BackgroundSessionManager.swift:29:17: error: stored property 'systemCompletionHandler' of 'Sendable'-conforming class 'BackgroundSessionManager' is mutable` and `SQLiteUploadStore.swift:42:28: error: cannot access property 'database' with a non-Sendable type 'OpaquePointer' from nonisolated deinit; this is an error in the Swift 6 language mode`. Local toolchain: Apple Swift 6.3.3 (swiftlang-6.3.3.1.3), target arm64-apple-macosx26.0, Xcode 26.6 (17F113). MODEL. BackgroundSessionManager now carries a '# Concurrency model' doc section with a six-row table (log / events / progress / lock / storedSession / storedCompletion), naming for each whether it is mutable, which threads touch it (private serial delegate queue, DispatchQueue.main, app-delegate launch, arbitrary async callers) and its protection. The two mutable fields are guarded by ONE NSLock — chosen over OSAllocatedUnfairLock/Mutex because ios/Package.swift is still .iOS(.v15). The session is created eagerly in init (it stays a var only because URLSession needs `self` as delegate, unavailable before super.init). `@unchecked Sendable` because the NSObject superclass forbids checked Sendable; the table is its justification. urlSessionDidFinishEvents now takes the handler and nils it in one critical section, retiring a latent double-call. WEAK EDGE KEPT: NetworkGovernor.start() is `{ [weak self] path in guard let self else { return }; Task { await self.update(from: path) } }` — the weak capture is unchanged and `guard let` only promotes the optional var to a let for the Task to capture; the doc comment records that stop() does not clear pathUpdateHandler, so dropping weak would leak. ADR-0002 INDEPENDENCE: `git diff --name-only` does not list ios/Package.swift (platforms: [.iOS(.v15), .macOS(.v14)] unchanged); TUSKitTransport.swift present and still returned by TransportSelector.select below iOS 17 (UploadTransport.swift:70-74, untouched); no @available/#available introduced. NO SILENCERS: grep over the added lines of the ios/.chief/.github diff for preconcurrency, nonisolated(unsafe), strict-concurrency=minimal, swiftLanguageVersions, swiftSettings, #if compiler prints 'no silencers in the diff'; -warnings-as-errors still present in ci.yml. BackgroundUploadStarter REMOVED rather than re-documented: BackgroundUploadStarting now refines Sendable, which both conformers meet (BackgroundSessionManager as above, the test double already @unchecked Sendable), so the wrapper's whole reason for existing is gone. Anchor line `        return TransferHandle(taskIdentifier: taskId, stagedRemainderPath: nil)` byte-identical — proven by the swift/I6 control running, not just by eye. AFTER. Same clean complete-checking build, fresh scratch: exit 0, 'Build complete! (7.56s)', ZERO lines matching error:|warning:. BackgroundSessionManager.swift:29 gone. SQLiteUploadStore.swift:42:28 FIXED rather than deferred (handle moved into a private final class Connection: @unchecked Sendable whose own deinit closes it; a class deinit is under no isolation). -strict-concurrency=complete deliberately NOT added to CI: it is clean on THIS toolchain only, and this tasklist exists because a local clean build does not predict CI's older compiler — adding an unverifiable flag would risk the very run US-3 has to prove green. GATES, all observed: `swift build --package-path ios -Xswiftc -warnings-as-errors` exit 0; `swift run --package-path ios AmphoraPathTests` exit 0, 'Amphora path tests: 46 passed (4 upload-path cases, 39 state-machine vectors, 3 I6 transport vectors)'; `Tests/Conformance/drift-control.sh --port swift` exit 0, 'drift-control: 2 control(s) ran, 0 skipped, 0 failure(s)' (the i6-01-fresh-transfer control went red naming 'I6 violated'); `.chief/tests/verify-skip-policy.sh` exit 0, 'all assertions passed'; `chief verify` exit 0 printing 'verify: 5 passed, 1 skipped, 0 failed' (recorded for tree dc677812b3f2d1ede7df93294071caf1efbe9669). NOT GREEN, per CLAUDE.md section 3: verify.sh reports `Gradle build — no JDK on PATH` as SKIPPED, so every Android claim on this branch is UNVERIFIED locally. And a local build cannot prove the two CI errors are gone, because CI's compiler is not this one; that proof is US-3's."
    },
    {
      "id": "US-2",
      "title": "The Room schema race is fixed where it starts: the plugin owns the directory, and the exported schema is committed",
      "acceptanceCriteria": [
        "Establish the mechanism before fixing it: confirm or refute, from evidence rather than plausibility, that `Empty schema file` comes from the two variant kapt tasks sharing one `room.schemaLocation`. Record: the concurrent start timestamps of `:android:kaptDebugKotlin` and `:android:kaptReleaseKotlin` in a run log, the schema path each writes, and what `Database.exportSchema` does with a pre-existing schema file in Room 2.6.1 (from the published Room sources or release notes — cite the URL). If refuted, name the real mechanism and fix THAT. Do not ship the plugin swap as a fix for a cause that was never established.",
        "The schema directory is configured by the Room Gradle Plugin: `id(\"androidx.room\")` at the SAME version as `room-runtime` / `room-compiler` (2.6.1), declared `apply false` in the root `build.gradle.kts` and applied in `android/build.gradle.kts`, with `room { schemaDirectory(\"$projectDir/schemas\") }`. The `kapt { arguments { arg(\"room.schemaLocation\", ...) } }` block is removed — two mechanisms writing one directory is the race again. The comment explaining why `exportSchema = true` is deliberate moves with it. No KSP migration here: kapt's `Falling back to 1.9` warning is real and out of scope, because that swap is a `replace` tasklist, not a fix.",
        "The exported schema for `UploadDatabase` version 1 (`UploadJob.SCHEMA_VERSION = 1`) is committed under `android/schemas/` at the path the plugin writes, and its bytes are Room's, not hand-written. There is no JDK on this machine, so obtain the file from CI: add a step to the `android` job, after `:android:assemble`, that fails when the generated schema differs from the committed one (for example `git status --porcelain -- android/schemas` non-empty => print the diff or the file, then exit 1). That step is both how the file is retrieved on the first push and the gate that stops a later entity change shipping without its schema. Room's docs say exported schemas belong in version control.",
        "The `android` job log of the proving run contains zero lines matching `room.schemaLocation` — run 34556064209 has one: `warning: The following options were not recognized by any processor: '[room.schemaLocation, kapt.kotlin.generated]'` on `:android:kaptDebugUnitTestKotlin` — and zero `Schema export directory` warnings. Record both counts. `kapt.kotlin.generated` is kapt's own option and may survive in a residual warning; if it does, quote the line and say so rather than calling the warnings gone.",
        "Do not read a green `android` job as evidence of this fix. The failure was nondeterministic and the job is green today without any change. The evidence is the established mechanism plus the configuration that removes it — the ROADMAP's 'fixed at the root (schema export configuration)'.",
        "GATES: in the `android` job of a pushed `chief/140-ci-green-at-the-root` CI run — `./gradlew --no-daemon --stacktrace :android:assemble` SUCCESS, `./gradlew --no-daemon :android:testDebugUnitTest` SUCCESS with `ConformanceVectorsTest` PASSED, and the new schema-drift step SUCCESS — quoted with the run id. Locally, `.chief/verify.sh` exit 0 with `Gradle build` SKIPPED (no JDK), recorded as unverified here, not green."
      ],
      "passes": true,
      "notes": "MECHANISM ESTABLISHED BEFORE FIXING — confirmed, not refuted. (a) Concurrent starts, from run 34556064209's `android` job log: `> Task :android:kaptReleaseKotlin` at 2026-09-11T02:51:02.2888336Z and `> Task :android:kaptDebugKotlin` at 02:51:02.2893492Z — 0.5 ms apart. In the failing run 33044503283 the same pair is `:android:kaptReleaseKotlin FAILED` at 2026-08-27T06:05:22.7150351Z and `:android:kaptDebugKotlin` at 06:05:22.9148274Z. (b) Schema path each writes: identical. Read from the published Room 2.6.1 sources (https://dl.google.com/dl/android/maven2/androidx/room/room-compiler/2.6.1/room-compiler-2.6.1-sources.jar), `Context.kt:150-178` resolves BOTH `schemaInFolderPath` and `schemaOutFolderPath` from the one `room.schemaLocation` value when the plugin's `room.internal.schemaInput`/`schemaOutput` are absent, and `DatabaseProcessingStep.kt:128-136` composes `<folder>/<qualifiedName>/<version>.json` for both — i.e. `android/schemas/dev.amphora.store.UploadDatabase/1.json` for debug AND release. (c) What exportSchema does with a pre-existing file: `Database.kt:106-119` — `if (inputFile.exists())` it deserializes it, and writes via `SchemaBundle.serialize(bundle, file)` = `FileOutputStream(file, false)` (room-migration-2.6.1-sources.jar, SchemaBundle.kt:77-79), which TRUNCATES. So the file is zero bytes for the width of a write, and the concurrent reader's `GSON.fromJson` returns null -> `?: throw IllegalStateException(\"Empty schema file\")` at SchemaBundle.kt:69, called from Database.kt:110. Those are the EXACT two frames of the observed stack (`SchemaBundle$Companion.deserialize(SchemaBundle.kt:69)` <- `Database.exportSchema(Database.kt:110)`), line number for line number. CONFIRMED. FIX. Root build.gradle.kts declares `id(\"androidx.room\") version \"2.6.1\" apply false` (same version as room-runtime/room-compiler); android/build.gradle.kts applies `id(\"androidx.room\")` and sets `room { schemaDirectory(\"$projectDir/schemas\") }`; the `kapt { arguments { arg(\"room.schemaLocation\", ...) } }` block is REMOVED and its exportSchema comment moved onto the `room {}` block. The two mechanisms are mutually exclusive by Room's own check — Context.kt:169-171 logs INVALID_GRADLE_PLUGIN_AND_SCHEMA_LOCATION_OPTION as an ERROR if both are set. Per RoomGradlePlugin.kt:139-154 the plugin gives each task its own `build/intermediates/room/schemas/<taskName>` output, leaves the committed directory as read-only INPUT, and both kapt tasks are `finalizedBy` one `copyRoomSchemas` task — no shared write target, no window. No KSP migration: kapt's `Falling back to 1.9` warning is real and untouched. SCHEMA COMMITTED, BYTES ARE ROOM'S. No JDK on this machine, so the file was retrieved from CI exactly as the criterion prescribes: the new `android` step `Exported Room schema matches the committed one` runs after `:android:assemble`, and on run 34675294998 it went red printing `?? android/schemas/` and the file Room wrote. Those 214 lines / 6760 bytes were committed verbatim as `android/schemas/dev.amphora.store.UploadDatabase/1.json` (formatVersion 1, database version 1, identityHash 14f26c2ffb9a86d9bf6401bf2584a425, 31 fields, no trailing newline — Gson's `toJson(writer)` emits none). Byte-equality is PROVEN, not assumed: on the next run the same step printed `android/schemas is clean: the exported schema equals the committed one`. LOG COUNTS on the proving run 34675398053, whole `android` job: lines matching `room.schemaLocation` = 0 (run 34556064209 had one); lines matching `Schema export directory` = 0. One warning SURVIVES and is quoted rather than called gone: `warning: The following options were not recognized by any processor: '[room.internal.schemaInput, room.internal.schemaOutput, kapt.kotlin.generated]'` on `:android:kaptDebugUnitTestKotlin` — RoomGradlePlugin.kt:96 configures `variant.unitTest` too, and no `kaptTest(\"room-compiler\")` is declared, so nothing claims those options. Also observed: `> Task :android:copyRoomSchemas NO-SOURCE`, which is the steady state working — an unchanged schema makes `exportSchema` return before writing, leaving the variant output dirs empty for the copy task's `@SkipWhenEmpty`. NOT CLAIMED: a green `android` job is not offered as evidence for this fix. That job was green on the unchanged tree too; the evidence is the mechanism above plus the configuration that removes it. GATES. Branch run 34675398053, head 076d82d: `Build Android library` (`./gradlew --no-daemon --stacktrace :android:assemble`) SUCCESS; `Exported Room schema matches the committed one` SUCCESS; `Run Android conformance vectors` (`./gradlew --no-daemon :android:testDebugUnitTest`) SUCCESS with `ConformanceVectorsTest > sharedVectorsMatchAndroidPort PASSED` and `ConformanceVectorsTest > sharedI6VectorsHoldForTheAndroidTransport PASSED`, `BUILD SUCCESSFUL in 19s`. Locally `chief verify` exit 0, `verify: 4 passed, 1 skipped, 0 failed` — and per CLAUDE.md section 3 that is NOT green: `Gradle build — no JDK on PATH` is SKIPPED, so every Android claim above rests entirely on the CI run quoted, never on this machine. (The count is 4 and not US-1's 5 only because `TypeScript dependency install` is conditional on an absent node_modules.) THE android JOB AS A WHOLE IS STILL RED, on a step this story does not own and did not touch — `Start tusd + MinIO` now fails with `pull access denied for minio/minio, repository does not exist or may require 'docker login'`. Diagnosed, not guessed: the Docker Hub registry answers 401 for BOTH the pinned digest `minio/minio@sha256:14cea493...` and for `minio/minio:latest`, so the repository itself is no longer anonymously pullable rather than the digest having rotted; `tusproject/tusd@sha256:6c5e17dc...` still answers 200, and `quay.io/minio/minio:latest` answers 200. That is US-3's first blocker and is recorded for it."
    },
    {
      "id": "US-3",
      "title": "One run id, every job green — and the record says exactly what still stands between this and Phase 1 closed",
      "acceptanceCriteria": [
        "Push the branch (`git push origin chief/140-ci-green-at-the-root`; `ci.yml` builds `chief/**` on push) and quote the run id with every job's conclusion from `gh run view <id> --json jobs --jq '.jobs[] | \"\\(.name) \\(.conclusion)\"'`. All five jobs — `ios`, `android`, `conformance-fixture`, `verify-policy`, `react-native` — are `success`.",
        "The `ios` job is observed REACHING and passing its later steps, not merely compiling: quote from that run's log the `Run Swift path tests` summary (`Amphora path tests: 46 passed ...`) and the `Swift drift negative control` summary (`2 control(s) ran, 0 skipped, 0 failure(s)`). Neither has ever executed in CI; their first execution is the deliverable.",
        "Re-run that run on the same head sha (`gh run rerun <id>`) and quote the second attempt's job conclusions. Two consecutive green attempts on identical input is the minimum this repository accepts from a gate that has flipped its verdict on identical input before. One is not enough.",
        "State Phase 1's exit condition in notes, verbatim from ROADMAP.md: 'one run id shows every job green, the `ios` job is observed reaching and passing its `swift run` and drift-control steps, and the Room kapt failure is either fixed at the root (schema export configuration) or reproduced on demand — a green run after a red one on unchanged input closes nothing.' Then list what the OPERATOR must do after the merge, because chief merges locally and makes no network call: (a) `git push origin main`; (b) `gh run list --branch main --limit 1`, then read that run — every job `success`, and `ios` reaching and passing `swift run` and the drift control; (c) `gh run rerun <id>` once and see it green again on the same sha; (d) only then mark the ROADMAP §1 'The CI gate is total' row and the Phase 1 section closed, citing both attempts.",
        "Update the record with what was observed and nothing more. ROADMAP.md's Phase 1 table and its §1 'The CI gate is total' row, and docs/reference/continuous-integration.md § Known divergence (the single home of the Swift error count, now zero), each cite the branch run id and both attempts. This tasklist does NOT mark Phase 1 closed — the `main` run has not happened. Answer the ROADMAP §4 open question 'Who owns `BackgroundSessionManager`'s concurrency model?' by pointing at the model US-1 wrote. Revise CLAUDE.md §3 'Corollary: CI's Swift is stricter than yours' so it no longer says the job is red today, and keep its warning that the two toolchains differ. Add a CHANGELOG `[Unreleased]` entry. Wherever a doc's claim changed, add a dated `## Corrections` entry (CLAUDE.md §6).",
        "GATES: `.chief/verify.sh` exit 0 (with `Gradle build` SKIPPED named, not green); `Tests/Conformance/check-single-fixture.sh` exit 0; the two CI attempts above, each quoted by run id and attempt number with all five job conclusions."
      ],
      "passes": true,
      "notes": "GATE GREEN, ONE RUN ID, BOTH ATTEMPTS. Run 34675666580, branch chief/140-ci-green-at-the-root, head 3b6f934dfd3dbaeedf96ec2d6302424c62e44755. `gh run view 34675666580 --json jobs --jq '.jobs[] | \"\\(.name) \\(.conclusion)\"'` — ATTEMPT 1: conformance-fixture success / ios success / react-native success / verify-policy success / android success (RUN success). ATTEMPT 2, after `gh run rerun 34675666580` on the IDENTICAL sha: android success / verify-policy success / conformance-fixture success / ios success / react-native success (RUN success, head 3b6f934). Two attempts because this gate has flipped verdict on unchanged input before; one is not enough. THE ios JOB REACHED ITS LATER STEPS — the deliverable, never executed in CI before. From attempt 1's log (`gh api repos/danieldekerlegand/amphora/actions/runs/34675666580/attempts/1/logs`): `Amphora path tests: 46 passed (4 upload-path cases, 39 state-machine vectors, 3 I6 transport vectors)` and `drift-control: 2 control(s) ran, 0 skipped, 0 failure(s)` (with `OK — red, and it named 'I6 violated'` above it). Attempt 2's log prints both lines identically. The same log names the compiler at last: `Apple Swift version 5.10 (swiftlang-5.10.0.13 clang-1500.3.9.4)`, `Xcode 15.4` — against Swift 6.3.3 / Xcode 26.6 locally, which is why the local gate never saw the two errors. android also produced `Kotlin real wire: 8 MiB uploaded across an aborted PATCH, resumed by a new client from server offset 6553600, checksum verified, peak extra disk 0 KiB` and `android/schemas is clean: the exported schema equals the committed one` on both attempts. ONE BLOCKER WAS FIXED TO GET HERE, inherited from US-2 and not in this story's original scope: `Start tusd + MinIO` failed with `pull access denied for minio/minio`. Re-diagnosed independently rather than taken on faith — registry-1.docker.io answers 401 for the pinned digest AND for `latest` on minio/minio (so the repository is gated, not the digest rotted) while tusproject/tusd answers 200, and quay.io returns docker-content-digest sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e for RELEASE.2025-09-07T16-13-09Z — byte-identical to the existing pin. So docker-compose.yml now pins quay.io/minio/minio at THE SAME DIGEST: the host changed, provably not the image. PHASE 1 EXIT CONDITION, verbatim from ROADMAP.md: 'one run id shows every job green, the `ios` job is observed reaching and passing its `swift run` and drift-control steps, and the Room kapt failure is either fixed at the root (schema export configuration) or reproduced on demand — a green run after a red one on unchanged input closes nothing.' THIS TASKLIST DOES NOT CLOSE PHASE 1 and does not claim to: both attempts are on a tasklist branch, and chief merges locally making no network call. WHAT THE OPERATOR MUST DO AFTER THE MERGE: (a) `git push origin main`; (b) `gh run list --branch main --limit 1`, then read that run — every job `success`, and the `ios` job observed reaching AND passing `swift run` and the drift control, not merely compiling; (c) `gh run rerun <id>` once and see it green again on the same sha; (d) only then mark ROADMAP §1's 'The CI gate is total' row and the Phase 1 section closed, citing both attempts. These four steps are written into ROADMAP Phase 1 itself, not only here. RECORD UPDATED, and only with what was observed: ROADMAP §1's row now reads 'GREEN ON A BRANCH — not yet on `main`' citing the run and both attempts; Phase 1 carries a Then/Now table and the operator steps; Phase 3's pin bullet gains the second failure mode (access withdrawn, not just tag withdrawn); §4's open question 'Who owns BackgroundSessionManager's concurrency model?' is struck through and ANSWERED — the type does, pointing at the `# Concurrency model` section US-1 wrote — while noting the same question has never been asked of the Kotlin port. docs/reference/continuous-integration.md § Known divergence, the single home of the Swift error count, now states ZERO with an empty table, records that the flag gap is closed (verify.sh passes -Xswiftc -warnings-as-errors) and that the TOOLCHAIN gap cannot be, quoting both compilers. CLAUDE.md §3's corollary no longer says the job is red today and keeps its warning that the two toolchains differ in both directions; §8's 'the CI gate is currently red' became 'green twice on a tasklist branch and has still never run green on main'. Dated `## Corrections` sections added to ROADMAP.md, CLAUDE.md and docs/guides/tusd-integration.md, and a new entry added to the existing one in continuous-integration.md — each saying what the doc said, what the tree says, what was read to tell them apart, and what the pass did NOT check. CHANGELOG [Unreleased] 2026-09-12 gains two entries (the green gate; the quay.io re-pin). LOCAL GATES: `chief verify` exit 0 — `verify: 4 passed, 1 skipped, 0 failed`, recorded for tree ceb45e1aeab588d89a97a053e47702a9aa41858e — and per CLAUDE.md §3 that is NOT green: `SKIPPED Gradle build: no JDK on PATH — UNVERIFIED, not passed` is named in its own summary, so every Android claim above rests on the CI runs quoted, never on this machine. `Tests/Conformance/check-single-fixture.sh` exit 0: 'one fixture at Tests/Conformance/vectors.json, referenced by both ports'. ADDENDUM: the record commit eaa71a3 was pushed as well, and run 34675934395 (head eaa71a3) is ALSO all five jobs success — so the branch head that gets merged carries its own green run, not only the code head 3b6f934 that the gates above quote."
    }
  ],
  "mergedToMain": "d0aa849",
  "gates": {
    "local": {
      "state": "passed",
      "detail": "the project verify hook (verify.sh) ran and exited 0"
    },
    "ci": [
      {
        "workflow": "CI",
        "state": "unknown",
        "detail": "a push of main can start it; whether it RAN for d0aa849 is not recorded here — chief makes no network call on the merge path. `chief cigate` is what asks GitHub."
      }
    ]
  }
}
