{
  "project": "amphora",
  "category": "unblock",
  "branchName": "chief/110-vectors-run-on-both-ports",
  "description": "**Forty conformance vectors exist and execute on one platform, which defeats the only purpose they have.** `Tests/Conformance/vectors.json` (40 vectors, schemaVersion 1) is read by both ports — `ConformanceTests.swift:6` and `ConformanceVectorsTest.kt:15` — and the whole reason it exists is to stop the Swift and Kotlin state machines drifting apart. A vector suite that runs on Swift only cannot detect Kotlin drift; it provides assurance without providing verification, which is worse than an acknowledged gap.\n\nTwo defects, both verified 2026-08-25:\n\n**The Kotlin side has never been compiled at all** — no JDK, no gradle, no wrapper, and no git remote, so CI never ran it either. `100` fixes that precondition and this tasklist depends on it.\n\n**Both sides load the vectors by a repo-root-relative path**, so running `swift run AmphoraPathTests` from `ios/` crashes with `NSCocoaErrorDomain 260` rather than failing a test. A fixture the runner cannot find is not a red test; it is a crash that reads as an environment problem, and it makes the suite's own working directory a hidden precondition.\n\n**Do not weaken the vectors to make them pass.** The design commitment they protect is real and specific: byte ranges stream from the source file with **no chunk temp files**, taking peak extra storage from roughly 2x the file size to about zero. `NativeResumableTransport.startTransfer` hands `URLSession` the original file with no slicing, and Kotlin must be shown to agree.",
  "dependsOn": [
    "100-android-into-ci"
  ],
  "touches": [
    "Tests",
    "ios",
    "android",
    ".github"
  ],
  "iters": 6,
  "warmup": [
    "python3 -c \"import json;d=json.load(open('Tests/Conformance/vectors.json'));print(type(d), len(d if isinstance(d,list) else d.get('vectors',[])))\"",
    "grep -n 'vectors.json' -r ios android Tests 2>/dev/null | head",
    "sed -n '1,25p' ios/Tests/*/ConformanceTests.swift 2>/dev/null || find . -name ConformanceTests.swift",
    "find . -name ConformanceVectorsTest.kt | head -1 | xargs sed -n '1,25p'",
    "cd ios && swift run AmphoraPathTests 2>&1 | tail -5"
  ],
  "userStories": [
    {
      "id": "US-1",
      "title": "The vectors load from anywhere, on both ports",
      "acceptanceCriteria": [
        "Vector loading is anchored to the repository root by a means that does not depend on the caller's working directory, on both the Swift and Kotlin sides.",
        "A missing or unreadable vector file FAILS a test with a legible message. It must not crash, and it must not silently pass — an absent fixture reporting green is the failure mode this whole batch is about.",
        "`swift run AmphoraPathTests` succeeds from the repository root AND from `ios/`. Both are recorded in the notes."
      ],
      "passes": true,
      "notes": "Swift: vectors resolved by walking up from #filePath; Kotlin: from the amphora.repoRoot system property Gradle injects (rootDir), falling back to walking up from user.dir — android/build.gradle.kts no longer moves the runner's workingDir. `swift run --package-path ios AmphoraPathTests` from the repo root: PASSES, 'Amphora path tests: 44 passed'. `swift run AmphoraPathTests` from ios/: PASSES, same line, exit 0 (it crashed with NSCocoaErrorDomain 260 before). Negative controls, both run from ios/: fixture moved away -> 'conformance vectors not found', 8 searched paths listed, exit 1, no trap; fixture rewritten with vectors:[] -> 'malformed: `vectors` is empty - zero vectors is a failure, not a pass', exit 1. Fixture restored, tree clean. .chief/verify.sh: 4 passed, 0 failed, 1 SKIPPED (Gradle - no JDK on this machine), so the Kotlin half of this story is UNVERIFIED locally and is first compiled by the android CI job; US-2 reports what it actually says."
    },
    {
      "id": "US-2",
      "title": "Kotlin executes the same 40 vectors, in CI",
      "acceptanceCriteria": [
        "The Kotlin vector test runs in the android CI job and its result is reported. This is the first execution of that test by anything, so report what it actually says rather than what it was expected to say.",
        "Both ports run the SAME file — one `vectors.json`, no per-platform copy. A second copy would reintroduce exactly the drift the vectors exist to catch.",
        "Vector count and schemaVersion are asserted, so a truncated or half-written fixture cannot pass as a full run. 'Ran 0 vectors' must be a failure, on the same principle that a test runner discovering zero tests is a failure and not a pass."
      ],
      "passes": true,
      "notes": "Kotlin ran in CI run 33040844045 (branch chief/110-vectors-run-on-both-ports), android job 98413854669: `ConformanceVectorsTest > sharedVectorsMatchAndroidPort PASSED`, alongside StorageGovernorTest PASSED and TusdIntegrationTest SKIPPED, BUILD SUCCESSFUL in 28s. Reporting what it says, not what it was meant to say: the android job is GREEN, the new conformance-fixture job is GREEN (`check-single-fixture: one fixture at Tests/Conformance/vectors.json, referenced by both ports`, 5s), verify-policy and react-native GREEN, and the ios job is RED — unchanged from run 33040087311 on the previous branch and unrelated to this commit: three pre-existing concurrency/Sendable errors (NetworkGovernor.swift:23, NativeResumableTransport.swift:16, TUSKitTransport.swift:27) that only the CI Swift surfaces and that docs/reference/continuous-integration.md already records as owned by nobody. One file: Tests/Conformance/vectors.json is the only vectors.json in the tree and Tests/Conformance/check-single-fixture.sh now gates that — negative controls run locally, all three fire (per-platform copy -> exit 1; copy RENAMED to ios/conformance-snapshot.json -> exit 1 on the row-01-enqueue sentinel; Kotlin port repointed at vectors-android.json -> exit 1); tree restored clean. Ran-zero-vectors closed on both ports: schemaVersion == 1, count == 40 asserted BEFORE the first reduction, unreduced (Enqueue) == 1 and reduced == 39. Swift negative controls, locally: fixture truncated to 18 -> 'Precondition failed: expected 40 vectors in .../vectors.json, got 18'; every row degenerated to Enqueue -> 'expected 1 non-reducing (Enqueue) vector, got 40'. The Kotlin assertions are structurally identical but their negative control has NOT been run — no JDK on this machine and CI only exercised the green path; US-3 owns driving both suites red. Local: swift build -warnings-as-errors 'Build complete', `swift run AmphoraPathTests` 44 passed from the repo root and from ios/, .chief/verify.sh 3 passed / 0 failed / 1 SKIPPED (Gradle, no JDK)."
    },
    {
      "id": "US-3",
      "title": "Drift is proven to be caught",
      "acceptanceCriteria": [
        "A deliberate divergence is introduced into ONE port and both suites are run: the vectors must go red. A drift detector nobody has watched detect drift is indistinguishable from one that cannot.",
        "The negative control is committed as a test or a documented procedure, not performed once and described in prose.",
        "The 'no chunk temp files' commitment is among the behaviours covered, since it is the design claim most likely to be quietly violated by a well-meaning port and the one with the largest consequence for the storage governor.",
        "Note in the docs which conformance properties the vectors do NOT cover, so their scope is legible rather than assumed total."
      ],
      "passes": true,
      "notes": "Both suites were driven RED by a deliberate divergence, and the control is committed as Tests/Conformance/drift-control.sh rather than performed once in prose. KOTLIN, IN CI (run 33042568129, android job): green path first - ConformanceVectorsTest > sharedVectorsMatchAndroidPort PASSED and > sharedI6VectorsHoldForTheAndroidTransport PASSED, BUILD SUCCESSFUL in 25s; then the control - state-machine divergence (SourceMissing reduced to BLOCKED/STORAGE_LOW instead of FAILED/SOURCE_GONE) -> sharedVectorsMatchAndroidPort FAILED, AssertionError at ConformanceVectorsTest.kt:144, named row-04-source-missing; I6 divergence (RangeRequestBody.writeTo writes a temp file before streaming) -> sharedI6VectorsHoldForTheAndroidTransport FAILED at ConformanceVectorsTest.kt:239, 'I6 violated'. Script verdict: 'drift-control: 2 control(s) ran, 0 skipped, 0 failure(s) ... every divergence introduced was caught by the vectors that were supposed to catch it'. SWIFT, LOCALLY (no ios CI - see below): the same two controls, both OK, 'drift-control: 2 control(s) ran, 0 skipped, 0 failure(s)'; the messages were 'Precondition failed: row-04-source-missing: expected failed, got blocked' and 'i6-01-fresh-transfer: I6 violated - 1 file(s) materialised during the attempt (vector.chunk), expected 0'. A control is only accepted when the suite exits non-zero AND prints the expected vector id, because a source file that no longer compiles also exits non-zero; mutations are restored byte-for-byte on every exit path and the script proves all four target files match the copies it took at startup. NO-CHUNK-TEMP-FILES IS NOW COVERED: schemaVersion 2 adds transportInvariants.i6NoChunkTempFiles, 3 rows both ports run against their own transport (Swift: NativeResumableTransport hands the session the original file, same path, same length, sizeBytes-resumeFrom expected bytes, no stagedRemainderPath, zero files created in a scratch dir installed as the process temp dir; Kotlin: RangeRequestBody streams exactly source[offset,size), declares it as Content-Length, creates no file). Local: swift build -warnings-as-errors 'Build complete'; 'Amphora path tests: 46 passed (4 upload-path cases, 39 state-machine vectors, 3 I6 transport vectors)'; .chief/verify.sh 3 passed, 0 failed, 1 SKIPPED (Gradle, no JDK). SCOPE IS WRITTEN DOWN: docs/reference/conformance-vectors.md lists what the vectors assert, the 10 expect keys the fixture states that NEITHER port reads (I2's persistBeforeTransfer among them - I2 is documented by a vector, not verified by one), the invariants with no vector at all (I3, I5, I8, I9), and the layers out of scope. TWO FIXES THE RUNS FORCED: (1) my first seam used nonisolated(unsafe), which is redundant-and-an-error on the CI Swift (run 33041928703) - replaced with a BackgroundUploadStarter wrapper, so ios is back to exactly its two pre-existing errors (NetworkGovernor.swift:23, TUSKitTransport.swift:27) and none of mine; (2) check-single-fixture.sh matched the sentinel vector id anywhere in a tracked .json, so chief's own notes about it turned that job red on de1b5cb - it now matches an id/value pair. Also raised the Gradle heap to 4g: kapt died with no diagnostic on 2 of 3 runs (33041928703, 33042386253) before the android job ever reached the vectors. FINAL RUN 33042568129: android SUCCESS, conformance-fixture SUCCESS, verify-policy SUCCESS, react-native SUCCESS, ios FAILURE on the two pre-existing Swift-concurrency errors that no story owns, so the Swift drift control does not execute on the runner - it is run locally and reported above."
    }
  ]
}
