# Chief Progress — 80-integration-against-tusd
Started: Thu Aug 20 02:39:27 CDT 2026
---

## US-1 — Both platforms complete an upload against a real tusd instance
- Added `integration/tusd/docker-compose.yml` with pinned tusd v2.4.0, MinIO, and S3 bucket initialization, plus `integration/tusd/run.sh` for a deterministic 3 MiB create/head/patch/version-mismatch/terminate smoke test.
- Added opt-in Swift (`AmphoraTusdIntegration`) and Kotlin (`TusdIntegrationTest`) live-wire clients that exercise create, authoritative HEAD, PATCH completion, pinned-version rejection, and post-DELETE 404/410 behavior.
- Documented setup and invocation in `docs/guides/tusd-integration.md` and linked it from README.
- Verification observed: Swift warnings-as-errors build green; Swift path tests 44 passed; Swift integration target compiles; shell syntax, Compose config, and diff checks green. Live Docker execution was attempted but the local Docker daemon was unavailable. Android Gradle was unavailable locally.

### Learnings for future iterations
- tusd's S3 credentials are supplied through `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, and `AWS_REGION`; the CLI does not take access-key flags.
- Keep real-wire tests opt-in because they require Docker and Android tooling; deterministic protocol/state checks remain the default CI path.
