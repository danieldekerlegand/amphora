# Chief Progress — 900-dead-code-paydown
Started: Thu Sep  3 05:27:52 CDT 2026
---

## Codebase Patterns

- **`verify.sh` local baseline is `3 passed, 1 skipped, 0 failed`.** The skip is
  `Gradle build — no JDK on PATH`. Per CLAUDE.md §3 that is NOT green: every Kotlin claim
  in a change rests entirely on CI. Say "3 passed, 1 skipped" in notes, never "green",
  unless the change touched no Kotlin (then say so explicitly).
- **Four cheap CI gates run locally with no toolchain** and are worth running on any change:
  `Tests/Conformance/check-single-fixture.sh`, `.chief/tests/verify-skip-policy.sh`,
  `npm --prefix packages/react-native run check:control-surface`,
  `Tests/Conformance/drift-control.sh --port swift`. The kotlin drift control needs a JDK.
- **Every new doc must be indexed in `docs/README.md`** ("unlinked is unreachable") and carry a
  `> **Status:** … · **Updated:** … · **Owner:** …` line. Bump the index's own Updated date too.
- **Reference-counting recipe used all through 900**, reusable for any symbol sweep:
  `grep -rhoE '(struct|class|enum|protocol|actor) [A-Z][A-Za-z0-9_]*' ios/Sources | awk '{print $2}' | sort -u`
  then per name `grep -rn --include='*.swift' -w <name> ios/ | grep -v '<decl form>' | wc -l`.
  Kotlin equivalent over `android/src`. Refcount 0 or 1 is the shortlist, not the answer.

---

## 2026-09-03 — US-1: An inventory, measured before anything is removed

WHAT I DID. Swept the whole tree for dead, stale and duplicated code and wrote the result to
`docs/reference/dead-code-inventory.md` (289 lines), indexed in `docs/README.md`. **Deleted
nothing** — US-1 is the measurement, US-2 is the removal.

FILES CHANGED: `docs/reference/dead-code-inventory.md` (new), `docs/README.md` (index entry +
Updated date). Commit `08fe3b0`.

VERIFY: `.chief/verify.sh` → 3 passed, 1 skipped, 0 failed (PASS Swift build, PASS Swift tests
46 passed, PASS TypeScript typecheck, SKIPPED Gradle — no JDK). Plus all four cheap CI gates
green. Docs-only, no Kotlin touched, so the Gradle skip strands nothing here.

THE SHAPE OF THE ANSWER — 6 genuinely dead, 8 deliberately-unexercised, 3 duplications,
4 undecidable classes. Full detail is in the doc; what matters for the next iterations:

**US-2 should start from §1 and expect it to shrink.** The six §1 findings are NOT equal:
- `loadResumeData` (§1.1) is the only one with a real cost attached, and it comes with a trap:
  the *write* half (`storeResumeData`, called at `DefaultUploadEngine.swift:298`) is live and
  leaks disk into a `resume_data` table nothing reads. Do NOT delete the writer to make the
  pair symmetric — that is a defect to report, not code to remove.
- `WireDialect.Rufh` Kotlin (§1.3) is dead by search and **contested by `wire-protocol.md`**,
  which documents it as one of two named dialects. Swift's `RufhDialect` is live. This needs a
  human ruling; do not delete it on the strength of a refcount.
- §1.4/§1.5/§1.6 are Kotlin and **cannot be verified on this machine at all** (no JDK). Any
  removal there is a CI-only claim. Weigh that against how little they are worth.
- §1.6 (three redundant `@RequiresApi`) is marginal and strands `androidx.annotation` if fully
  removed. Probably leave it; record why, which is what US-2's third criterion asks for.

**§2 is the more valuable half of the artifact** and is exactly what stops the next sweep
re-litigating the same files. Read it before touching anything that looks unused. The
headline: ten `EnumConverters` `@TypeConverter` methods report **zero** call sites each and
the module cannot compile without them — kapt writes the calls, and kapt runs nowhere here.

**§3.1 is the finding with real teeth.** The six-arm HTTP-status→`ErrorClass` table is written
out verbatim twice in Swift (`BackgroundSessionManager.swift:212-223` and
`ControlPlaneClient.swift:124-132`), read on two different paths, and covered by no vector. A
change to one copy ships a build that retries `429` on HEAD and gives up on it mid-transfer
with every test still green. If US-2 does one refactor, do this one.

LEARNINGS FOR FUTURE ITERATIONS.
- **The tree is disciplined; do not expect a big harvest.** Zero commented-out code blocks in
  any source language, zero unused Swift imports, one TODO — and that TODO names future work
  rather than hiding an abandoned path. The findings are small and specific by nature.
- **A zero refcount answers a different question than "is this dead".** Three of the four
  blind spots in §4 actually occur here (kapt codegen, framework dispatch, external
  consumers); only reflection does not. Every framework callback in the tree —
  `urlSession(...)`, `onAvailable`/`onLost`/`onCapabilitiesChanged`, `onResponse`/`onFailure`,
  `doWork` — reports zero call sites and is mandatory.
- **Cross-port asymmetry is the strongest dead signal available in this repo**, and it cuts
  both ways. `inState` is Kotlin-only and uncalled → stray. `renewLease`/`pruneCompleted` are
  uncalled in *both* ports → a specified surface, not an accident. And Swift's
  `stageIfRequired` is uncalled while its Kotlin twin IS called → the **caller** is missing,
  which is port drift, not dead code.
- **A REAL BUG FOUND, out of scope here, worth a tasklist.** `DefaultUploadEngine.swift:178-253`
  never calls `sources.stageIfRequired(job)`, so a `ph://` job hands
  `URL(fileURLWithPath: "ph://...")` straight to the background session. Kotlin does call its
  twin at `DefaultUploadEngine.kt:217`. The iOS Photos-asset path is broken.
- **A second live gap.** `nativeResumeUnavailable` (`DefaultUploadEngine.swift:28`) is never
  inserted into and never read; the whole five-file `noteNativeResumeSupported` chain is a
  no-op. `wire-protocol.md:57-63` specifies the behaviour it should implement — the silent
  degradation to non-resumable upload that the doc calls "a metric worth alerting on".
- **FOR THE 901 DOCS TASKLIST:** `docs/guides/tusd-integration.md` exists and is **not indexed
  in `docs/README.md`**, whose own rule is "unlinked is unreachable". Left alone deliberately —
  900's brief says do not fix docs beyond what a removal strands.
- Research doc: none for this tasklist (`research` not set). The map above is the substitute.

---

## 2026-09-03 — US-2: Removal, with the tree still green and the reason recorded

WHAT I DID. Acted on the US-1 inventory in **four commits, one finding each**, then wrote the
disposition record. Four removals went in; **three of the six §1 "genuinely dead" candidates did
not**, and one of those three did not because *the inventory's own evidence for it was wrong*.

FILES CHANGED
- `ios/Sources/Amphora/Transport/ControlPlaneClient.swift`, `Session/BackgroundSessionManager.swift`
  — `afabf05`, the §3.1 dedupe.
- `android/src/main/kotlin/dev/amphora/store/UploadDao.kt` — `ac91b10`, `inState` + its stranded
  `UploadState` import.
- `android/src/main/kotlin/dev/amphora/work/UploadNotifications.kt` — `9edd129`, the `SDK_INT >= O`
  guard.
- `android/build.gradle.kts` — `6d16ac7`, the `core-ktx` declaration.
- `docs/reference/dead-code-removal.md` (new), `docs/reference/dead-code-inventory.md` (disposition
  markers), `docs/README.md`, `CHANGELOG.md` — `5bed8ac`.

VERIFY. `.chief/verify.sh` after every commit: **3 passed, 1 skipped, 0 failed** (PASS Swift build,
PASS Swift tests 46 passed, PASS TypeScript typecheck, SKIPPED Gradle — no JDK). All four cheap CI
gates green on the final tree. **Three of the four removals are Kotlin and rest entirely on CI** —
that is written into the commit messages, the removal record §4, and the CHANGELOG entries.
**No test was deleted, weakened or adjusted.** The suite runs the same 46 cases before and after.

THE FINDING THAT MATTERS MOST FOR 901 AND BEYOND — **US-1's §1.1 corroboration row was false.**
It claimed `grep -rn "resume_data\|resumeData\|ResumeData" docs/ Tests/ packages/ integration/`
"returns nothing". Re-run verbatim it returns **four lines**: `ios-background-transfer.md:65-66`
("we persist the blob **before** reporting the error, since its presence means 'resumable' rather
than 'start over'") and `platform-constraints.md:21-22`, which names the consumer
`uploadTask(withResumeData:)`. `loadResumeData` is therefore the unimplemented **read** half of a
specified iOS 17 capability — the same shape as §2.4's `nativeResumeUnavailable` chain — and belongs
in §2. It was NOT removed. The disk leak §1.1 flagged is real and unchanged: the fix is to implement
the reader, not to delete it.

**Why that grep result was wrong is worth knowing:** `uploadTaskResumeData` contains the substring
`ResumeData`, so the pattern *should* have hit. The claim was recorded, not run. That is the single
most dangerous failure mode in this tasklist — a search that is *quoted* rather than executed reads
exactly like one that was.

ALSO NOT REMOVED, both recorded in the removal record with reasons:
- `WireDialect.Rufh` (Kotlin). `wire-protocol.md:5-9` — "Two dialects, one transport interface" with
  a column-per-dialect table naming `Rufh`, written to span both ports; Swift's `RufhDialect` is
  live. Deleting the Kotlin half turns a specification into a lie. Needs a decision: implement the
  construction site, or amend the doc to say RUFH is iOS-only.
- The three `@RequiresApi(O)`. True documentation, and the only thing keeping `androidx.annotation`
  on the classpath.
- Duplications §3.2 (two Swift transports) and §3.3 (`.offsetAdvanced`) deferred: the first crosses
  the seam I6's three transport vectors watch and the transports' *difference* IS I6; the second is
  state-machine policy pinned by all 40 vectors.

LEARNINGS FOR FUTURE ITERATIONS.
- **Re-run the search before you act on a quoted one.** Two of six §1 rows survived contact with
  their own evidence; one of them because the evidence was never executed. Checking all six cost
  minutes. Deleting `loadResumeData` would have erased the only in-code trace of a documented iOS 17
  capability, and nobody re-reads a deleted file.
- **"Specified but unimplemented" and "dead" look identical to grep and are opposites.** The
  discriminator is always a *document*, never a refcount. Both survivors in this pass were resolved
  by reading `docs/reference/`, not by counting references. When a symbol's zero-caller status is
  the whole case for deletion, grep the docs for the *mechanism* it implements, not just its name.
- **A correction to a prior story's artifact belongs IN that artifact, struck through.** The
  inventory's §1.1 rows are struck through in place with the correction beside them, not silently
  rewritten — matching how CHANGELOG.md handles tasklist `80`. A rewritten artifact loses the record
  that the method can fail this way.
- **One commit per finding is cheap here and worth it.** Each of the four is a few lines; the value
  is that the three CI-only Kotlin ones can be reverted individually if CI disagrees, without
  touching the one change that was actually verified locally.
- **Kotlin markdown/table gotcha, mechanical:** inserting a paragraph directly above a Markdown table
  with no blank line between silently stops the table rendering. Bit me once in the inventory.

STILL OPEN FOR 901 (unchanged from US-1, restated so it is not lost): `docs/guides/tusd-integration.md`
is not indexed in `docs/README.md`. And two live defects found by this sweep and deliberately NOT
fixed here: `DefaultUploadEngine.swift` never calls `stageIfRequired` (iOS `ph://` path broken, Kotlin
does call its twin), and the `resume_data` table is written and never read.
