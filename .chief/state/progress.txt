# Chief Progress — 60-ios-completes-remainder-and-photo-sources
Started: Thu Aug 20 02:31:02 CDT 2026
---

## US-1 — `writeRemainder` streams from the source and re-samples pressure

Implemented chunked FileHandle range copying in `StorageGovernor`, with injected capacity sampling for deterministic tests. A pressure shortfall throws `StorageError.pressureRose` and removes the destination via cleanup defer. Added successful offset-copy and mid-write pressure-collapse tests in `UploadPathTests`.

Files changed: `ios/Sources/Amphora/Governor/StorageGovernor.swift`, `ios/Tests/AmphoraTests/UploadPathTests.swift`, runtime and tracked PRDs.

Learnings for future iterations: `StorageGovernor` is an actor, so direct tests must be async and capacity behavior is best exercised through its `@Sendable` provider; run the Swift package checks from the repository root so conformance vectors resolve correctly.
